apiVersion: batch/v1
kind: Job
metadata:
  creationTimestamp: null
  labels:
    run: math-add-job
  name: math-add-job
spec:
  completions: 3 # 3 desired and successful pods
  parallelism: 3
  template:
    spec:
      containers:
      - image: ubuntu
        name: math-add-job
        command: ['expr', '3', '+', '2']
      restartPolicy: Never
